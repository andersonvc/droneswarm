[workspace]
members = ["drone-lib", "wasm-lib", "rl-train"]
resolver = "2"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"

# WASM size optimization (moved from wasm-lib/Cargo.toml)
[profile.release.package.wasm-lib]
opt-level = "s"
